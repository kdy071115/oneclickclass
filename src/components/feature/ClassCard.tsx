import { ChevronRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { ClassItem } from '../../types/class'; import { StatusBadge } from '../common/StatusBadge';
export function ClassCard({item}:{item:ClassItem}){return <Link className="class-card" to={`/classes/${item.id}`}><span className="class-thumb" style={{background:`linear-gradient(135deg,${item.color},color-mix(in srgb, ${item.color}, white 35%))`}}/><span className="class-info"><StatusBadge>{item.status}</StatusBadge><strong>{item.title}</strong><small>{item.type} · {item.date}</small>{item.status==='모집중'&&<><span className="progress"><i style={{width:`${item.enrolled/item.capacity*100}%`,background:item.color}}/></span><small>신청 {item.enrolled} / {item.capacity}명</small></>}</span><ChevronRight size={18} color="#c4cbd3"/></Link>}
