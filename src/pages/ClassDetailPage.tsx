import { Award, BarChart3, CheckSquare, ClipboardList, Eye, Settings, Users } from 'lucide-react'; import { Link, useParams } from 'react-router-dom'; import { PageHeader } from '../components/common/PageHeader';
const menus=[['applicants',Users,'신청자','24명 · 결제대기 3명'],['attendance',CheckSquare,'출석 관리','오늘 2회차'],['survey',BarChart3,'설문','응답 18명'],['exams',ClipboardList,'시험','시험 2개']] as const;
export function ClassDetailPage(){const {id='notion'}=useParams();return <div className="page subpage class-dashboard"><PageHeader title="노션으로 시작하는 업무 자동화"/><span className="badge blue">모집중 · 온라인</span><p className="muted">8월 5일 시작 · 신청 24 / 30명</p><div className="dashboard-grid">{menus.map(([path,Icon,title,desc])=><Link to={`/classes/${id}/${path}`} key={path}><Icon/><b>{title}</b><small>{desc}</small></Link>)}</div><Link className="wide-menu certificate-menu" to={`/classes/${id}/certificates`}><i><Award/></i><span><b>수료증 관리</b><small>수료 조건과 발급 설정</small></span></Link><Link className="wide-menu" to={`/classes/${id}/manage`}><i><Settings/></i><span><b>강의 설정</b><small>공개 상태 · 정원 · 신청서 수정</small></span></Link><Link className="preview-link" to={`/classes/${id}/preview`}><Eye/>수강생에게 보이는 화면</Link></div>}
