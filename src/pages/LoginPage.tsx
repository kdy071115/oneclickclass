import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { StatusBar } from '../components/common/StatusBar';
export function LoginPage(){const nav=useNavigate();const [values,setValues]=useState({id:'',password:''});const [error,setError]=useState('');function submit(e:FormEvent){e.preventDefault();if(!values.id.includes('@'))return setError('올바른 이메일을 입력해 주세요.');if(values.password.length<6)return setError('비밀번호는 6자 이상 입력해 주세요.');nav('/');}return <main className="standalone framed"><section className="auth page"><StatusBar/><h1>안녕하세요 :)<br/>원클릭 클래스예요</h1><p className="muted">로그인하고 강의를 관리해보세요</p><form onSubmit={submit}><label>이메일 아이디<input type="email" placeholder="example@email.com" value={values.id} onChange={e=>setValues({...values,id:e.target.value})}/></label><label>비밀번호<input type="password" placeholder="비밀번호를 입력하세요" value={values.password} onChange={e=>setValues({...values,password:e.target.value})}/></label>{error&&<p className="form-error">{error}</p>}<button className="primary" type="submit">로그인</button></form><div className="auth-links"><button>아이디 찾기</button><i/> <button>비밀번호 찾기</button><i/><Link to="/signup">회원가입</Link></div><div className="sns-divider"><span>SNS 계정으로 로그인</span></div><div className="sns"><button>◼</button><button>N</button><button>f</button><button>G</button></div><Link className="guest-link" to="/guest">비회원으로 신청 조회하기</Link></section></main>}
